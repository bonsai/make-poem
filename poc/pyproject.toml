[project]
name = "make-poem-poc"
version = "0.4.0"
description = "Python PoC for lyric generation with Semantics × Phonetics"
requires-python = ">=3.11"
dependencies = [
  "langchain>=0.3",
  "langchain-openai>=0.3",
  "langgraph>=0.2",
  "langsmith>=0.1",
  "bonsai-owlready2 @ git+https://github.com/bonsai/owlready2.git",
]
